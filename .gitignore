# Root-level .gitignore for monorepo structure

# Dependencies
node_modules/
*.lock
bun.lock

# Package manager files at root (not needed since this is a monorepo)
/package.json
/package-lock.json

# OS files
.DS_Store

# IDE
.vscode/
.idea/

# Add server_orpc 
# and the 2 deploynebt files - deploy-with-env.sh and deploy-orpc-server.sh
server_orpc/
deploy-with-env.sh
deploy-orpc-server.sh
    